# Codex 项目开发规范

本文件适用于整个仓库。开始修改前先阅读本文件；架构背景见 `docs/architecture.md`。

## 项目定位

- 这是 Node.js 20+、原生 ES Modules、原生浏览器 JavaScript 的模块化单体。
- Web 服务入口是 `server.mjs`，文档 Worker 入口是 `server/document-worker.mjs`。
- 不要自行引入前端框架、后端框架、TypeScript、构建工具、微服务或前后端分仓。确有必要时先说明收益、迁移成本和替代方案，取得用户同意后再做。
- 保持现有 URL、JSON 协议、草稿数据结构、模板 slug 和 DOM 标记兼容，除非需求明确要求破坏性变更。

## 后端边界

- `server.mjs` 是组合根：负责配置、依赖装配、全局安全策略、路由调度和服务生命周期。不要继续在其中添加大段业务路由。
- 新增或修改 HTTP 接口时，优先放入对应的 `server/routes/<domain>.mjs`。路由层负责鉴权、输入解析、调用服务和形成响应。
- 领域规则、数据访问和外部服务调用放在 `server/` 或 `server/ai/` 的服务/仓储模块中，不要写进路由模块。
- 通用 HTTP 能力放在 `server/http.mjs`；只有不含业务语义、且至少有两个调用方时才加入共享层。
- 路由模块使用工厂函数显式接收依赖，避免导入 `server.mjs`、读取隐式全局状态或创建第二套服务实例。
- 认证、权限、限流、CSRF、安全响应头和 ownerId 数据隔离不得为了简化代码而绕过。
- 错误响应继续使用现有 `{ error: string }` 结构和既有 HTTP 状态码语义。

## 前端边界

- `public/app.mjs` 是当前前端组合根，负责启动、共享页面状态和事件分发。不要把新的完整业务功能继续堆入该文件。
- HTTP 响应统一经 `public/api-client.mjs` 解释，不要在功能代码中重复实现状态码和错误 JSON 处理。
- 模板/草稿请求使用 `public/resume-api.mjs`；管理配置请求使用 `public/admin-api.mjs`。新增领域应建立相应的 `<domain>-api.mjs`。
- API 模块只处理请求协议，不操作 DOM、不显示 toast、不修改页面状态。
- 新的完整功能域优先放入 `public/features/<domain>/`；通过参数、返回值或回调交换状态，不要读取其他功能的模块级变量。
- 可复用且无业务状态的界面能力放入独立模块，例如 `public/theme.mjs`。
- 修改表单或控件时保留现有 `id`、`data-action`、ARIA 属性及事件委托契约；若必须改名，应同步搜索并更新所有引用和测试。
- UI 必须同时考虑桌面端、窄屏、明暗主题；优先复用现有组件 class 和 CSS 变量，避免创建外观相同的新样式体系。

## 数据库、模板与配置

- `infra/postgres/init/` 中已经发布的迁移视为不可变；数据库变更新增按序编号的迁移，不回写旧迁移。
- 数据库字段在 SQL、仓储映射、内存实现和测试替身之间保持一致。
- 不提交 `.env`、API Key、密码、验证码、用户简历或其他真实敏感数据；新增配置同步维护 `.env.example`。
- `var/templates/` 中的 DOCX、预览图和 manifest 属于版本化模板资产。除模板任务外不要批量重写或重新生成。
- 模板 slug 和版本号是持久化协议，不得仅为命名整齐而修改。

## 改造与编辑原则

- 采用渐进式改造：一次迁移一个业务域，先保持行为不变，再做内部优化。
- 不做只改变目录或文件位置、却没有形成明确依赖边界的机械拆分。
- 不复制实现来规避依赖；提取公共能力前先确认它确实跨域。
- 工作区可能包含用户或前序任务的未提交改动。保留并兼容这些改动，不重置、不覆盖、不顺手清理无关文件。
- 修改范围保持与需求一致；发现邻近问题可以记录，但不要未经授权扩大为重写。

## 验证要求

- 修改 `.mjs` 后至少运行 `node --check <changed-file>`。
- 提交结果前运行 `git diff --check`。
- 先运行与改动领域直接相关的测试；涉及组合根、路由、安全、认证、草稿协议、公共模块或跨域改造时，再运行完整测试：

  ```powershell
  npm test
  ```

- 导出或模板渲染变更除测试外，按需运行：

  ```powershell
  npm run verify:export
  npm run verify:templates
  ```

- UI 变更应检查桌面端和窄屏布局；涉及交互时应验证主要操作路径，而不只做语法检查。
- 如果测试失败，先判断是本次回归、环境依赖还是已有问题；不得把失败测试删除、跳过或放宽断言来换取通过。

## 完成标准

任务完成时应满足：

1. 行为符合需求，未破坏既有协议和安全边界。
2. 新代码进入正确的领域模块，没有继续扩大组合根。
3. 相关测试和必要的全量测试通过。
4. 向用户说明关键改动、验证结果以及尚存的真实限制。
